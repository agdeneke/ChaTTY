Server:

- Add user commands.
	- /list (DONE)
	- /me (DONE)
	- /help
- Fix username being marked as used.
- Fix messages only being broadcast every other message.

Client:

- Prevent control characters from being printed when using arrow keys.
- Log chat window in client.
- Wait for client user to send SIGINT before exiting client.

Both:
- Log timestamps.
- Optimize code for performance.
- Check string bounds.
- Check for race conditions.
